`OOP - Object Oriented Programming`

Object : Key:Value
Class/classes : Blueprint - Template - Reference - heriditary (kindof) [same key with unique/diff values]

<!-- 4 basic things -->

Class defines how an object should be and object is created based on that class

create a class

```python
class [name]:
    x=5
```

create object

```python
p1 = myClass()
print(p1.[key])
```

Practice

```python
class myClass:
    x=5
p1 = myClass()
print(p1.x)
```

- delete object usig `del` keyword

- `pass` keyword allows you to create an empty class

```python
class myClass:
    pass
```

`init` method

- it is a built-in method `__init__()`

- `decorator`
- `generator`
- `lambda`
