# Task: Make a list of numbers [5, 10, 15, 20]. Print:

# The total sum of the numbers using sum()
# The average of the numbers

numbers = [5, 10, 15, 20]

print("Total sum of the numbers", sum(numbers))
print("The average of the numbers", sum(numbers)/len(numbers))